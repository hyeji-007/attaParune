<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.attaparune.admin.AdminMapper">
    <insert id="insRestaurant">
        INSERT INTO `admin`
           SET `role_id` = #{roleId}
             , aid = #{aid}
             , apw = #{apw}
             , `name` = #{name}
             , email = #{email}
             , phone = #{phone}
    </insert>
</mapper>